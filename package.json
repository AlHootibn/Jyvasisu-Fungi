{
  "name": "farmiq",
  "version": "1.0.0",
  "description": "IoT Mushroom Farm Management System",
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && node index.js",
    "install:all": "cd client && npm install && cd ../server && npm install"
  }
}
